<aside class="span3" id="tagbar">
<legend>Tags</legend>
  <div id="tags">
    <% Tag.all.each do |tag| %>
      <ol><i class="icon-tags"></i> <%= link_to tag.name, tag_path(tag) %></ol>
    <% end %>
  </div>
</aside>
