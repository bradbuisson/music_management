<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title><%= full_title(yield(:title)) %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Future Home For Samuel Raines Music" %>">
    <meta name="author" content="<%= content_for?(:author) ? yield(:author) : "Artist: Samuel Guy Raines, Designer: Nathan Carl Couch" %>">
      <%= auto_discovery_link_tag(:atom, { controller: :articles, action: :feed }, { title: "Sam Raines Music News" } ) %>
      <%= auto_discovery_link_tag(:atom, { controller: :blogs, action: :feed }, { title: "Sam Raines Music Blog" } ) %>
      <%= stylesheet_link_tag "application", media: "all" %>
      <%= javascript_include_tag "application" %>
      <%= csrf_meta_tags %>
      <%= render 'layouts/shim' %>
      <%= yield(:head) %>
    </head>
    <body>
      <%= render 'shared/modal' if signed_in? %>
      <div class="masthead">
      <h1 class="muted logo hidden-phone"> A Beautiful Logo </h1>
        <div class="navbar">
          <div class="navbar-inner">
            <div class="container">
              <%= render 'layouts/navigation' %>
            </div>
          </div>
        </div>
      </div>
      <div class="page-wrapper" id="main" role="main">
        <%= render 'layouts/messages' %>
        <%= yield %>
      </div>
      <footer class="footer">
        <div class="container">
          <%= render 'layouts/footer' %>
      </div>
    </footer>
  </body>
</html>
